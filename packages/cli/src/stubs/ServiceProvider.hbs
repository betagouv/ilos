import { Container, Parents } from '@ilos/core';

@Container.serviceProvider({
  providers: [], // Declare your providers
  handlers: [], // Declare your handlers
  children: [], // Declare sub service providers here  
})
export class {{name}}ServiceProvider extends Parents.ServiceProvider {}
