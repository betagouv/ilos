import { Container } from '@ilos/core';
import { ProviderInterface } from '@ilos/common';

@Container.provider({
  // identifier: {{name}}ProviderInterfaceResolver
})
export class {{name}}Provider implements ProviderInterface {}
